@model List<ResultEmployeeDTO>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Admin/Index.cshtml";
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Personel Listesi</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Personel Adı</th>
                                <th scope="col">Ünvan</th>
                                <th scope="col">Durum</th>
                                <th scope="col"></th>
                                <th scope="col"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                int count = 1;
                            }
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">@count</th>
                                    <td>@item.EmployeeName</td>
                                    <td>@item.Title</td>
                                    @{
                                        <td>@(item.Status ? "Aktif" : "Pasif")</td>
                                    }
                                    <td><a href="/Admin/Employee/UpdateEmployee/@item.EmployeeID" class="btn btn-outline-success">Güncelle</a></td>
                                    <td><a href="/Admin/Employee/DeleteEmployee/@item.EmployeeID" class="btn btn-outline-danger">Sil</a></td>
                                </tr>
                                count++;
                            }
                        </tbody>
                    </table>
                    <a href="/Admin/Employee/CreateEmployee" class="btn btn-outline-info">Yeni Personel Girişi</a>
                </div>
            </div>
        </div>
    </div>
</div>

